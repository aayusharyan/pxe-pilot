# Runtime dependencies only. Install to run the app (or use Docker).
# Used by Dockerfile, docker-compose, and production; also by venv for local run.
flask>=3.0,<4
gunicorn>=21.0,<23
sqlalchemy>=2.0,<3
